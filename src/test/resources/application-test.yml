spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/graduateam  # localhost 대신 127.0.0.1 사용
    username: graduateam
    password: graduateam!
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  logging:
    level:
      org.springframework: WARN